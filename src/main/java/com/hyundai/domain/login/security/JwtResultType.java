package com.hyundai.domain.login.security;

/**
 * fileName      : JwtResultType
 * author        : 변형준
 * since         : 2/20/24
 * 내용           :
 */
public enum JwtResultType {
    VALID_JWT, EXPIRED_JWT, INVALID_JWT, NO_JWT
}
